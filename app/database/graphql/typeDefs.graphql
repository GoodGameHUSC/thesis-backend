# import Post from './type/post.graphql'
# import Comment from './type/comment.graphql'
# import Users from './type/users.graphql'

type Query {
  products: [Post],
  product: [Post],
  
}

type Mutation {
  login: [Post] 
}